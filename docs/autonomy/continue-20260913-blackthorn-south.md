# 검은먹 남쪽 45·46·29번도로 구현 인계 — 2026-09-13

## 변경

- 검은먹 남문 `(16,54)`에서 성도45번도로 → 46번도로 → 29번도로 동쪽 합류부로 이어지는 실제 맵과 왕복 워프를 추가했다.
- MapId와 크기는 `tour_johto_route_45`40×96, `tour_johto_route_46`36×72, `tour_johto_route_29`80×32다.
- 45·46번도로에 남향 일방 턱과 별도 동쪽 귀환 오르막을 만들었다. 29번도로는 동쪽 합류부까지만 구현해 현재 없는 연두마을·무궁시티를 임의로 만들지 않았다.
- 검은먹 안내원·센터·주민 기록·남쪽 표석과 상위 이동/맵 크기 문서를 현재 연결에 맞췄다.

## 검증

- `tests/johto-blackthorn-south.test.ts`: 3/3 통과.
- `npm.cmd run build`: 최초 구현 시 통과. 최종 재실행은 별도 작업 범위 `src/sinnoh-celestic-life.ts:11`의 선택적 PC 배열 타입 오류로 중단됐다.
- 관련 파일 `git diff --check`: 통과.
- 브라우저 자연 보행·화면·저장·음향 QA는 실행하지 않았다.

## 남은 문제와 다음 작업

- 45번도로 꼬마돌 Lv.23~25, 46번도로 꼬렛·깨비참·꼬마돌 Lv.22~24 선택 조우를 생성 데이터에 반영했다. 두 도로 선택 트레이너와 현지 포획 동료/승리 상태 안내도 실제 이벤트에 연결했다.
- 후속 집중 검사5/5와 전체 빌드가 통과했다. 브라우저 QA는 아직 실행하지 않았다.
- 45·46번도로의 층리 절벽·밝은 일방 턱·45번 산물길·움직이는 선택 풀밭과29번 합류부 평지 언덕을 전용 DP풍 시각층으로 적용했다. 집중 검사6/6과 전체 빌드가 통과했다.
- 미니맵 너비/높이 축척, 세 도로의 공식 번호 출구 마커, 검은먹과 여행자의 풀밭→선택 실전→다음 도로/귀환 목적지 안내를 적용했다. 집중 검사8/8과 전체 빌드가 통과했다.
- 남쪽 묶음은 계획 구현 완료 후보다. 다음은 검은먹 남문부터29번도로 경계까지 자연 보행·포획·두 선택 실전·귀환·저장·화면·음향을 도시 QA로 묶어 확인한다.
- 29번도로 나머지 구간과 연두마을·무궁시티는 별도 도시 작업 단위로 남긴다.
- 남쪽 도로 묶음의 계획된 구현이 끝난 뒤 검은먹 남문부터29번도로 경계까지 자연 보행·왕복·저장·화면·음향 QA를 모아서 수행한다.
