# 검은먹시티·용의굴 집중 QA — 2026-09-12

## 변경

- `REGIONAL_IMPLEMENTATION_DIRECTIVES.md` 2.1의 8개 사이트 역할을 확인하고, HGSS 검은먹 구간의 정확한 참고 URL·원작 사실·프로젝트 변경·MapId·검증 경계를 성도 문서와 출처 대장에 기록했다.
- 원본 세이브를 건드리지 않는 `tests/johto-blackthorn-browser-*-prepared-save.json` 준비 사본을 사용했다. 게임 코드와 공통 저장 계약은 변경하지 않았다.

## 검증

- `npm.cmd run build`: 통과. Vite 청크 크기 경고만 있었다.
- 44번도로 준비 사본: 야생 덩쿠리 Lv.24를 꼬부기 Lv.25의 물의파동 두 번으로 약화하고 몬스터볼로 포획했다. 화면의 파티 수가 1→2로 바뀌고 `덩쿠리가 파티에 등록되었다`를 확인했다.
- 검은먹센터 준비 사본: HP 4/80 꼬부기를 간호사에게 회복시켰다. PC가 `파티 1/6 · 박스 1/60`을 표시했고 X 취소로 필드에 복귀했다. 슬롯 1 저장 후 같은 `검은먹시티 · 포켓몬센터` 상태를 재불러왔다.
- 용의굴 준비 사본: 남쪽 출구를 걸어 `tour_johto_dragons_den`→`tour_blackthorn` 귀환을 확인했다. 사당 준비 사본은 `tour_johto_dragons_den_shrine`→`tour_johto_dragons_den` 귀환을 확인했다. 검은먹 외부·용의굴·사당의 전용 화면을 관찰했다.
- 소리 버튼은 `소리 켜기`에서 `소리 끄기`로 바뀌어 활성화 상태를 확인했다. 도구가 실제 출력음을 들을 수 없어 음향 청취 통과로 기록하지 않는다.

## 남은 문제

- 수정 빌드의 별도 준비 사본에서 황토 동문→44번도로→얼음샛길 1F/B1F/B2F/B3F→1F 동쪽→검은먹 서문을 개발 지도 이동 없이 실제 방향 입력으로 연속 완주했다. 도착 상태 슬롯 저장·재불러오기와 콘솔 경고·오류 0건을 확인했다.
- 찬물 호흡 이후 전승관 암반 단계의 PC 보관·기절 보류, PC 데려오기·센터 회복, 건강한 파티 복귀 뒤 완료와 슬롯 저장 재접속은 확인했다. 호수→섬→사당 전체 흐름의 브라우저 연속 수행은 준비 위치별 확인과 자동 저장 파싱 근거만 있으며 단일 자연 진행 근거는 아니다.
- HGSS 원작의 얼음 미끄럼·괴력 바위, 이향 체육관·장로 시험·라이징배지·특별 미뇽, 수상·낚시는 현재 미구현이다.

## 다음 작업

- 검은먹을 `구현 완료 후보·도시 QA 보완 중`으로 유지한다.
- 실제 출력 음향은 청취 가능한 환경에서 확인한다. 구현은45·46·29번도로 남쪽 연결로 이어간다.

## 후속 변경 — 북쪽 용의굴 안내 충돌

- 실제 검은먹 북문·용의굴·사당 워프가 열린 상태와 달리 안내원·센터·북쪽 경계석이 북쪽을 미개통으로 설명하던 런타임 문구를 수정했다.
- 브라우저에서 안내원이 `서쪽 얼음샛길과 북쪽 용의굴·전승 사당은 실제 왕복`, 북쪽 길잡이석이 `용의굴 지하 호수와 전승 사당 왕복 가능`이라고 표시하는 것을 확인했다. 남쪽45번도로 미개통과 장로 시험·배지·특별 포켓몬 보상 미구현 경계는 유지한다.
- `npm.cmd run build`와 관련 파일 `git diff --check`를 통과했다.

## 후속 변경 — 동료 재선택 저장 경계

- 새 동료로 찬물 호흡을 다시 시작할 때 이전 동료의 `dragonsDenLakeObserved`, `dragonsDenIslandObserved`, `dragonsDenObservationCompleted`가 남던 결함을 수정했다. 암반 비교를 포함한 후속 동료 기록을 새 선택과 함께 초기화한다.
- `tests/johto-blackthorn-slice.test.ts`에서 44번도로·얼음샛길 전 층의 양쪽 워프 사이 보행 연결, 동료 재선택 초기화, 전체 활동 완료의 저장 파싱과 무보상 상태를 확인했다. 집중 검사 3/3 통과, 빌드 통과다.
- 브라우저에서 선택 주뱃의 PC 보관·기절 상태가 진행을 보류하는지, 검은먹센터 PC 데려오기와 간호사 회복, 건강한 파티 복귀 뒤 암반 비교 완료, 슬롯 저장·재불러오기 후 완료 기록 재확인을 확인했다. 준비 사본 기반 기능 QA이며 자연 여행 근거가 아니다.
- 이 단계 작성 당시 남아 있던 황토부터 검은먹까지 단일 자연 보행은 아래 후속 변경에서 완료했다. 실제 음향 청취만 남아 있다.

## 후속 변경 — 연속 이동 중 렌더 오류 복구

- 황토 안내소와 동쪽 표석의 옛 축약 통로 문구를 현재 `44번도로→얼음샛길 전 층→검은먹` 왕복 안내로 정정했다.
- 얼음샛길 1F/B1F와 절구산 산행객이 로드되지 않는 `hiker` 스프라이트를 사용해 `image.height` 렌더 오류가 발생하던 결함을 기존 `ace_trainer_m` 자산으로 수정했다.
- 수정 뒤 집중 검사3/3·빌드·변경 공백 검사를 통과하고, 황토부터 검은먹까지 실제 방향 입력 연속 이동·도착 슬롯 저장 재접속·콘솔 무오류를 확인했다. 실제 음향 청취만 남아 있다.
