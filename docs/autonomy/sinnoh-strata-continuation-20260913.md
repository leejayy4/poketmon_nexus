# 천관산 현장·봉신 표본 시각 연결 — 2026-09-13

- 변경: `src/sinnoh-strata-art.ts`에 기존 두 지층의 공통 표현을 구현하고, `sinnoh-celestic-art.ts` 현장 조사 칸과 `sinnoh-celestic-ruins-art.ts` 전시 표본에서 직접 호출한다.
- 기존 현장 표본은 색이 다른 작은 직사각형에 그쳤다. 서쪽은 끊어진 층리·엇갈린 균열, 동쪽은 연속된 수평층·밝은 세로 물자국으로 구분한다. 전시 왼쪽/오른쪽 표본에도 같은 표현을 사용한다. 원작의 신규 지질 설정이 아닌 기존 프로젝트 관찰 대사의 표현이다.
- 현장 좌표 하드코딩을 제거하고 `coronet211WestLayer` / `coronet211EastLayer` props의 실제 조사 칸을 읽는다. 표현은16×16 셀 안에 한정하며 보행 가능한 현장은 낮게 드러난 바닥 암반으로 유지한다.
- 지도·크기·NPC·보행·워프·진행·저장·보상을 변경하지 않았다. 기능과 렌더 호출은 기존 경로에 연결했다.
- 검증: 지정 세 지침·최신 인계·Git 상태·소스를 읽고 수정했다. QA 중단으로 테스트·빌드·브라우저·저장/시청각 검증은 실행하지 않았다. 이번 변경은 구현·미검증이다.
- 남은 문제/다음 시작점: 현장 한 칸 표본의 실제 축소 가독성은 미검증이다. 다음 continue는 `sinnoh-celestic-route.ts`와 `sinnoh-celestic-art.ts`의 천관산 통과층 출입구 표현을 읽고, 서부/동부 귀환 방향이 보이는 입구 보수를 진행한다. 중앙 PROJECT_STATE·다른 지방 인계 미수정.
