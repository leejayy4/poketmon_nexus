/** Current NEXUS rules, not BW/BW2 canonical rules or the long-term campaign balance. */
export const RUNTIME_RULES = Object.freeze({
  levelCap: 25,
  partyCapacity: 6,
  boxCapacity: 60,
  moveCapacity: 4,
  inventoryCapacity: 999,
  purchaseQuantityLimit: 99,
});

