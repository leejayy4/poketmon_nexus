import type { GameMap } from './types';
export function extendSinnohWestRoute(map:GameMap):void{const width=64,height=28,rows=Array.from({length:height},()=>Array.from({length:width},()=>'#'));const open=(x:number,y:number,w:number,h:number)=>{for(let j=y;j<y+h;j++)for(let i=x;i<x+w;i++)rows[j][i]='.';};
open(1,12,62,3);open(8,7,3,8);open(9,6,16,3);open(22,7,3,8);open(30,12,3,10);open(31,19,17,3);open(45,12,3,10);open(52,7,3,8);open(53,6,9,3);
map.width=width;map.height=height;map.walkable=rows.map(row=>row.join(''));const west=map.warps.find(w=>w.entry==='left'),east=map.warps.find(w=>w.entry==='right');if(west)Object.assign(west,{x:1,y:12});if(east)Object.assign(east,{x:62,y:12});map.terrain=[{kind:'tallGrass',x:12,y:6,w:7,h:3},{kind:'tallGrass',x:35,y:19,w:7,h:3},{kind:'tallGrass',x:54,y:6,w:5,h:3}];map.props.push({x:6,y:10,dialogue:'sinnohWestRouteSign'},{x:56,y:10,dialogue:'sinnohWestRouteSign'});map.npcs.push({id:'sinnohWestResearcher',name:'수로 조사원',sprite:'scientist_f',x:31,y:18,facing:'down',dialogue:'sinnohWestResearcher'});}
