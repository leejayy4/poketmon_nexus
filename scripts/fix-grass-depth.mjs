import fs from 'node:fs/promises';let s=await fs.readFile('src/renderer.ts','utf8');s=s.replace("if(map.id==='town')for(let y=TOWN_GRASS.y;y<TOWN_GRASS.y+TOWN_GRASS.h;y++)for(let x=TOWN_GRASS.x;x<TOWN_GRASS.x+TOWN_GRASS.w;x++)paintTallGrass(c,x*16,y*16,true,g.clock*2+x,this.images['grass-reference']);","if(map.id==='town'){const x=Math.round(pos.x),y=Math.round(pos.y);if(x>=TOWN_GRASS.x&&x<TOWN_GRASS.x+TOWN_GRASS.w&&y>=TOWN_GRASS.y&&y<TOWN_GRASS.y+TOWN_GRASS.h)paintTallGrass(c,x*16,y*16,true,g.clock*2+x,this.images['grass-reference']);}");await fs.writeFile('src/renderer.ts',s);
