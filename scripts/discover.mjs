const r=await fetch('https://api.github.com/repos/pret/pokeplatinum/git/trees/main?recursive=1'); const j=await r.json(); await import('node:fs/promises').then(fs=>fs.writeFile('scripts/platinum-tree.json',JSON.stringify(j))); console.log((j.tree||[]).filter(x=>/\.png$/.test(x.path)&&/overworld|player|hero|npc|field|furniture|building/.test(x.path)).map(x=>x.path).slice(0,140).join('\n')); console.log('entries',j.tree?.length);
